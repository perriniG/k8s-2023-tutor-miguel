apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    project: proxy-server
    environment: testing
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
